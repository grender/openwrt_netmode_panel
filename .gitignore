# Сборочный вывод: бинарь пересобирается `make build`, в истории не нужен.
/build/

# Артефакт панели (internal/httpapi/panel/) НЕ игнорируется намеренно:
# go:embed — зависимость времени компиляции, и без него `go build` на чистом
# клоне потребовал бы фронтенд-инструментов (ADR-0036).

# Мусор macOS. Появляется сам, поэтому глушится, а не удаляется руками.
.DS_Store
