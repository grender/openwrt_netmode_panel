# Сборочный вывод: бинарь пересобирается `make build`, в истории не нужен.
/build/

# web/vendor НЕ игнорируется намеренно: панель работает без шага сборки,
# и htm-preact-standalone.module.js обязан ехать вместе с исходниками.
