Авто-пул на живом роутере. 2026-09-19, 192.168.9.1, mihomo v1.19.29,
nikki 2026.04.08-r1, netmoded 0.2.0-phase2-20260919 (ветка feat/autopool).

Перехват выключен всю проверку: nikki.proxy.enabled=0 и mixin.tun_enabled=0,
в журнале «[Proxy] Disabled.». Из таблиц nft только inet fw4, правил
маршрутизации 1024/1025 нет, tun-устройства нет — проверялось после КАЖДОГО
шага, включая перезапуск движка самим демоном.

== Профиль новой раскладки принят ==
  провайдеры: sub, sub-auto (оба file, один путь)
  группы:     AUTO (url-test), PROXY (select), BYPASS (fallback)
Склейка собрала это в /etc/nikki/run/config.yaml без правок.

== Режим deny: исключили Альфу и пять «РЦ» ==
  PUT /api/nikki/autopool → 202, джоб autopool
  движок НЕ перезапущен: netmode.main.mode=off, и демон намеренно не
  поднимает движок, который владелец выключил. После ручного restart:

  sub:      38 узлов
  sub-auto: 32 узла
  выпали ровно шесть:
    🇦🇬💳Альфа
    🇨🇻⚪Чарли (РЦ-1)☁️   🇨🇻⚪Чарли (РЦ-2)☁️
    🇳🇷⚪Новембер (РЦ-3)☁️      🇳🇷⚪Новембер (РЦ-4)☁️
    🇧🇧⚪Браво (РЦ-5)☁️
  живых в пуле: 24 из 32
  AUTO выбрала 🇧🇧⚡Браво — НЕ Альфу. Исходный дефект закрыт.

== Ручной пин узла ВНЕ пула ==
  POST /api/nikki/proxy {"name":"🇦🇬💳Альфа"} → 200
  selected=🇦🇬💳Альфа fixed=🇦🇬💳Альфа pinned=true auto_pool=32
  Ради этого пулы и разводились: авто сужено, ручной выбор по всем 38.

  Возврат: {"name":"AUTO"} → 200, selected=🇧🇧⚡Браво (узел, а не слово
  «AUTO»), pinned=false. Строк с именем AUTO в списке: 0. Всего строк 40
  (1 «Авто» + 38 узлов + 1 разделитель).

== Конвертер на живой подписке ==
Манифест писал старый демон, поля pool в нём не было (provider_pool: 0).
После POST /api/subscription/update — 38 узлов, ok, и в манифесте:
  🇦🇶 🚀Авто | Быстрый узел ⚡⚡ → 12 имён
  Дельта-1, Дельта-2, Чарли-3, Чарли-4 EXTRA, Гольф-1,
  Отель, Браво, Лима, Эхо, Джульетта, Кило, Фокстрот
Совпало с тем, что было выведено вручную из сырой подписки (raw/94).

== Режим provider через путь демона ==
netmode.main.mode=nikki (при proxy.enabled=0 перехват всё равно невозможен),
PUT {"mode":"provider"} → 202 → демон САМ перезапустил движок через
netmode-apply.
  sub: 38   sub-auto: 12
  AUTO: участников=12, now=🇨🇻⚡Чарли-3
  перехват: не появился

== Обновление подписки при неизменившемся составе ==
  pid mihomo до: 1713, после: 1713 — перезапуска НЕ было.
  sub-auto остался 12.

== Отказы ==
  устаревший If-Match  → 409 stale_autopool
  режим «как-нибудь»   → 400 bad_request
  имени нет в подписке → 400 unknown_node
  allow с пустым списком → 400 bad_autopool
    «Авто-пул не принят: в авто-пуле не осталось ни одного узла»
  Файл отказами не тронут: auto=provider на месте.

== Обе секции в одном файле ==
  строка состояния несёт и sets=…, и auto=provider
  в теле три рубрики: proxy-providers, rule-providers, nikki-rules

== Оставлено ==
Служба погашена, автозапуск снят, proxy.enabled=1 и tun_enabled=1 возвращены,
netmode.main.mode=off. Профиль новой раскладки и auto=provider оставлены:
это рабочая конфигурация, меняется из панели.
