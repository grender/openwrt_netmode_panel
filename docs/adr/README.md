# Журнал архитектурных решений

Каждый файл — одно решение в формате **Статус / Контекст / Решение /
Последствия / Рассмотренные альтернативы**. Решения приняты после разведки
(`docs/recon/`), поэтому ссылаются на реальные ответы роутера, а не на
предположения.

Правило чтения: **если ADR противоречит `SPEC-backend-router-panel.md` —
побеждает ADR.** Спека осталась базой, но часть её решений отменена
владельцем осознанно; см. [ADR-0018](0018-spec-superseded.md).

| № | Решение | Отменяет / уточняет |
|---|---|---|
| [0001](0001-layering.md) | Слои и направление зависимостей | SPEC §4 |
| [0002](0002-source-of-truth.md) | Источник правды — UCI и живая система | SPEC §12 |
| [0003](0003-saved-networks-in-wireless.md) | Сохранённые сети живут в `/etc/config/wireless` | **отменяет SPEC §4 Слой 1** |
| [0004](0004-selection-derived.md) | Выбранная сеть выводится из `disabled` | **отменяет `option upstream`** |
| [0005](0005-section-name-identity.md) | Идентификатор сети — имя секции | — |
| [0006](0006-no-rollback.md) | Автоматического отката нет | **отменяет SPEC §6** |
| [0007](0007-b4-api-only.md) | b4 — только через HTTP API | **снимает SPEC §3.2** |
| [0008](0008-b4-exclusive-sets.md) | Эксклюзивность сетов — наша семантика | — |
| [0009](0009-phase1-write-invariant.md) | Инвариант записи фазы 1 | **заменён [0026](0026-switch-from-ambiguous.md)** — фаза 2 пишет `disabled` в двух путях. Часть текста 0009 сохраняется дословно (создание секции всегда с `"1"`, правка активной запрещена) |
| [0010](0010-never-auto-repair.md) | Неоднозначность докладывается, не чинится | — |
| [0011](0011-optimistic-concurrency.md) | Оптимистичная блокировка `wireless` по отпечатку | — |
| [0012](0012-write-only-credentials.md) | Пароли только на запись (`has_key`) | уточняет SPEC §7 |
| [0013](0013-fast-vs-slow.md) | Быстрые и медленные операции разделены | сохраняет SPEC §5 |
| [0014](0014-lan-bind-and-token.md) | Bind только на LAN, токен в сайдкаре 0600 | уточняет SPEC §12 |
| [0015](0015-executor-shape.md) | `Executor` без универсального `Run` | **отменяет SPEC §10** |
| [0016](0016-upstream-deferred.md) | Смена upstream отложена, `501` | **отменяет SPEC §11 п.7**; **мандат исполнен [0025](0025-upstream-narrow-verb.md)** — блокер снят, `POST /api/upstream` работает и `501` больше не отдаёт |
| [0017](0017-polling-no-push.md) | Опрос 1 Гц, без WebSocket и SSE | сохраняет SPEC §7 |
| [0018](0018-spec-superseded.md) | SPEC §4, §6, §10, §11 заменены этим набором | — |
| [0019](0019-radio-role-derived.md) | Привязка «радио ↔ роль ↔ диапазон» выводится из системы | уточняет [0009](0009-phase1-write-invariant.md) |
| [0020](0020-netmode-apply-exit-codes.md) | `netmode-apply` различает отказы кодами возврата | уточняет [0015](0015-executor-shape.md) |
| [0021](0021-panic-recovery-in-goroutines.md) | Фоновые горутины перехватывают панику общим помощником | уточняет [0006](0006-no-rollback.md), [0010](0010-never-auto-repair.md) |
| [0022](0022-external-output-limits.md) | Вывод внешних команд ограничен, stdout и stderr — по-разному | уточняет [0015](0015-executor-shape.md) |
| [0023](0023-nikki-panel-secret-on-demand.md) | Секрет Clash API уходит одним ответом по клику, а не в статусе | уточняет [0012](0012-write-only-credentials.md); **частично отменён [0024](0024-panel-links-only-when-service-answers.md)** |
| [0024](0024-panel-links-only-when-service-answers.md) | Кнопка веб-морды движка рисуется, только когда движок отвечает | **частично отменяет [0023](0023-nikki-panel-secret-on-demand.md)** (довод «ссылка — адрес, а не проба живости») |
| [0025](0025-upstream-narrow-verb.md) | Смена upstream: узость операции вместо отката | **исполняет мандат [0016](0016-upstream-deferred.md)**; подтверждает [0006](0006-no-rollback.md) |
| [0026](0026-switch-from-ambiguous.md) | Переключение разрешено из `ambiguous`; инвариант записи фазы 2 | **заменяет [0009](0009-phase1-write-invariant.md)**; уточняет [0010](0010-never-auto-repair.md) |
| [0027](0027-netmode-wifi-exit-codes.md) | `netmode-wifi` различает отказы кодами и не судит об исходе | уточняет [0015](0015-executor-shape.md), по образцу [0020](0020-netmode-apply-exit-codes.md) |
| [0028](0028-uci-revert-not-rollback.md) | `uci revert` — отмена своего черновика, а не откат | уточняет [0015](0015-executor-shape.md), [0011](0011-optimistic-concurrency.md); **не ослабляет** [0006](0006-no-rollback.md) |
| [0029](0029-panel-budget-revised.md) | Бюджет панели — 80 КБ gzip вместо 60, порог внимания 64 | **заменяет число из SPEC §1**; не отменяет правило «панель без сборки и npm» |

## Механические гейты

Часть решений защищена скриптами: они валят сборку, если решение начали
отменять в коде. Это важнее текста — текст можно не прочитать.

| Решение | Гейт |
|---|---|
| 0006 | `scripts/check-no-rollback.sh` |
| 0007 | `scripts/check-b4json.sh` |
| 0005, 0019 | `scripts/check-no-hardcoded-if.sh` |
| 0009 | `scripts/check-disabled-write.sh` — **удалён** вместе с решением ([0026](0026-switch-from-ambiguous.md)): он стерёг формулировку «`disabled` пишется ровно в одном месте и только `"1"`», которой больше нет, и оставленный запрещал бы фазу 2 механически |
| 0026 | `scripts/check-wireless-write.sh` — **написан и стоит в `make checks`**, правила `R1`–`R10`. Грепом держатся: `UCIDelete("disabled")` не встречается нигде, `disabled` пишется только литералами `"0"`/`"1"`, мест записи ровно три в двух файлах (создание с `"1"`, гашение прочих `"1"`, включение целевой `"0"`), `ApplyUpstream` зовут из одного места. Порядок записей, единственность коммита, поведение при `Ambiguous` и таксономию исходов греп **не** проверяет — их держат Go-тесты, и зелёный здесь про них не говорит ничего |
| 0025, 0027 | `scripts/check-netmode-wifi.sh` — **написан и стоит в `make checks`**: гоняет копию скрипта на подставных `ubus`/`wifi`/`flock`, сценарии `S1`–`S13`. Стережёт пару «скрипт ↔ сценарии» и **не сверяет пять копий таблицы кодов** между собой — см. врезку ниже |
| 0025 | `scripts/check-fail-reasons.sh` — таксономия **причин** провала сходится в пяти источниках (Go, OpenAPI, обе локали `i18n.js`, `app.js`, мок). Единственный гвард фазы 2, который сверяет копии текста между собой |
| 0028 | **гейта нет** — граница «только на пути отказа, только до коммита, только по своей секции» грепом не выражается, держится тестами. Девятая причина `stale_draft` попала под `check-fail-reasons.sh`, но это про её наличие в пяти списках, а не про границу полномочий |
| 0020 | `scripts/check-netmode-apply.sh` |
| 0029 | `scripts/check-web-size.sh` — числа берутся из переменных `limit`/`warn`, а не пишутся словами рядом |
| — | `scripts/check-evidence.sh` (любой внешний литерал) |
| — | `scripts/check-netmode-seed.sh` (именованность `netmode.main`) |
| 0023 | `scripts/check-evidence.sh` (шаблон `/ui/`), `scripts/check-routes.sh` (маршрут) — **но не само правило** |
| 0024 | **гейта нет и теста нет** — правило живёт в разметке панели. Косвенно: `scripts/measure-geometry.mjs` (`make geometry`, вне `verify`) меряет геометрию панели, но про показ кнопок не знает ничего |
| 0001 | `scripts/check-layering.sh` — **ещё не написан** |
| 0021, 0022 | **гейта нет** — держатся на тестах (`internal/safe`, `internal/executor`) |

> **Гвард есть у причин, а не у кодов, и путать их дорого.**
> `check-fail-reasons.sh` сверяет пять источников **таксономии причин**
> (`apply_failed`, `busy`, `stale_draft`, …) и валит сборку при расхождении.
> Таблица **кодов возврата** `netmode-wifi` (`0`/`1`/`3`/`4`/`7`) тоже живёт
> в пяти местах — шапка скрипта, `upstreamSentinels`,
> [`docs/contracts/executor.md`](../contracts/executor.md), таблица в
> [0027](0027-netmode-wifi-exit-codes.md), сценарии `check-netmode-wifi.sh` —
> и **её синхронность не проверяет никто**. Соседство двух строк в этой
> таблице читается как «значит, стерегут обе». Не стерегут.

Полный список охранников с описанием — в README проекта. Здесь только те, что
стоят за решениями из этого журнала.

Строка «0021, 0022» — не оговорка, а признание, и два решения в ней держатся
по-разному. Лимиты вывода сторожит `TestLimitCoversRealRouterOutput`: он
перевыводит запас из снятых фикстур при каждом прогоне и покраснеет сам, когда
запас съест реальность. А вот новая горутина, написанная без `safe.Do`, не валит
ничего и выглядит на ревью нормально — здесь правило держится на дисциплине.
Гейт для неё написуем и не написан
([ADR-0021](0021-panic-recovery-in-goroutines.md)).

Строка «0023» оговорена по той же причине. Оба названных гейта настоящие и
валят сборку, но стерегут они **обстановку**, а не само решение:
`check-evidence.sh` требует, чтобы шаблон адреса чужой панели был подтверждён
разведкой, `check-routes.sh` — чтобы новый маршрут был описан в
`docs/api/openapi.yaml` и совпадал с тем, что дёргает панель. Собственно правило
«секрет уходит только этим одним ответом» держат тесты —
`TestStatusNeverLeaksSecret` и `TestNikkiPanelNeverLogsSecret`. Они поймают
секрет, положенный в новое поле статуса, и не поймают нового эндпоинта, который
станет отдавать его вторым путём.

Строка «0024» — самая слабая в таблице, и записана она такой намеренно. Правило
«кнопку рисуем, только когда служба отвечает» живёт в одном логическом «и»
в разметке шапки. Го-тесты стерегут **поле** `links`, а не показ кнопки;
фронтенд-тестов в проекте нет вовсе. Правка, вернувшая кнопку в прежний вид, не
уронит ничего и на ревью выглядит нормально — ровно то же признание, что и в
строке «0021, 0022». Единственная механика рядом — `scripts/check-panel-sync.sh`:
он следит, чтобы правило не разошлось между `web/` и `internal/httpapi/panel/`,
но про само правило не знает ничего.
