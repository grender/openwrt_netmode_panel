module netmoded

go 1.22

// Зависимостей нет и не будет. Только стандартная библиотека.
// Это не пожелание, а проверяемое требование: scripts/check-stdlib.sh
// валит сборку на первом же нестандартном импорте — раньше и точнее,
// чем это сделает check-size.sh на пределе в 10 МБ.
require ()
